# 개인 로컬 설정 (PC마다 다름, 공개 불필요)
.claude/settings.local.json
.claude/settings.local.json.tmp.*

# 서버 실행 로그 (실행할 때마다 바뀜)
*.log

# 강사 참고용 에이전트 (저장소에 안 올림 — 내가 만든 ksk-react-dev.md는 유지)
.claude/agents/single-react-dev.md

# Windows 바로가기
*.lnk

# OS / 정크 파일
.DS_Store
__MACOSX/

# Playwright 테스트 부산물
.playwright-mcp/

# API 키 환경파일 (절대 커밋 금지)
.env
**/.env

# DB 자격증명 메모 (평문 비밀번호 포함 — 절대 커밋 금지)
week_4/supadb.txt

# node 의존성
**/node_modules/
